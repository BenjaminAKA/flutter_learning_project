import 'package:flutter/material.dart';

class Lesson07LocalStorage extends StatelessWidget {
  const Lesson07LocalStorage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
